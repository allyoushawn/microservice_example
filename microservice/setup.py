from setuptools import setup, find_packages

setup(
    name="microservice",
    version="1.0",
    description="Microservice example performing sentiment analysis",
    include_package_data=True,
    packages=find_packages()
)