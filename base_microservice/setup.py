from setuptools import setup, find_packages

setup(
    name="base_microservice",
    version="1.0",
    description="Library for a base implementation of a FastAPI application",
    packages=find_packages()
)
